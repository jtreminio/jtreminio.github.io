#!/bin/bash
set -e
set -o pipefail

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
IMAGE="nginx:alpine"

docker run -d --name jtreminio-blog \
    -v ${DIR}/../public:/usr/share/nginx/html:ro \
    -p 46353:80 \
    ${IMAGE}
